/** Frescura de la configuración remota: se revalida al volver al primer plano. */
export const REMOTE_CONFIG_STALE_TIME_MS = 30 * 60 * 1000;

export const REMOTE_CONFIG_QUERY_KEY = ["remote-config"] as const;
